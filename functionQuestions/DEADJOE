
*** These modified files were found in JOE when it aborted on Thu Dec 24 19:42:35 2020
*** JOE was aborted by UNIX signal 1

*** File '(Unnamed)'
addingTwoBinary.cpp

*** File 'addingTwoBinary.cpp'
#include <bits/stdc++.h>
using namespace std;

int reverse(int n)
{
    int ans = 0;
    while (n > 0)
    {
        int lastDigit = n % 10;
        ans = ans * 10 + lastDigit;
        n /= 10;
    }
    return ans;
}

int addingTwoBinary(int a, int b)
{                              
    int ans = 0;
    int prevCarry = 0;

    while (a > 0 && b > 0)
    {
        if (a % 2 == 0 && b % 2 == 0)
        {
            ans = ans * 10 + prevCarry;
            prevCarry = 0;
        }
        else if ((a % 2 == 0 && b % 2 == 1) || (a % 2 == 1 && b % 2 == 0))
        {
            if (prevCarry == 1)
            {
                ans = ans * 10 + 0;
                prevCarry = 1;
            }
            else if (prevCarry == 0)
            {
                ans = ans * 10 + 1;
                prevCarry = 0;
            }
        }
        else if (a % 2 == 1 && b % 2 == 1)
        {
            if (prevCarry == 0)
            {
                ans = ans * 10 + prevCarry;
                prevCarry = 1;
            }
            else if (prevCarry == 1)
            {
                ans = ans * 10 + prevCarry;
                prevCarry = 1;
            }
        }
        a /= 10;
        b /= 10;
    }

    while (a > 0)
    {
        if (prevCarry == 1)
        {
            if (a % 2 == 1)
            {
                if (prevCarry == 1)
                {
                    ans = ans * 10 + 0;
                    prevCarry = 1;
                }
            }
            else
            {
                ans = ans * 10 + 1;
                prevCarry = 0;
            }
        }
        else
        {
            ans = ans * 10 + (a % 2);
        }
        a /= 10;
    }

    while (b > 0)
    {
        if (prevCarry == 1)
        {
            if (b % 2 == 1)
            {
                if (prevCarry == 1)
                {
                    ans = ans * 10 + 0;
                    prevCarry = 1;
                }
            }
            else
            {
                ans = ans * 10 + 1;
                prevCarry = 0;
            }
        }
        else
        {
            ans = ans * 10 + (b % 2);
        }
        b /= 10;
    }

    if (prevCarry == 1)
    {
        ans = ans * 10 + 1;
    }

    //now reversing what we got

    ans = reverse(ans);
    return ans;
}

int main()
{
    int a, b;
    cin >> a >> b;

    cout << addingTwoBinary(a, b) << endl;
}
*** File '* Startup Log *'
Processing '/etc/joe/joerc'...
Processing '/etc/joe/ftyperc'...
Finished processing /etc/joe/ftyperc
Finished processing /etc/joe/joerc
